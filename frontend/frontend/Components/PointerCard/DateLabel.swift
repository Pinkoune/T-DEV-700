import SwiftUI

struct DateLabel: View {
    var body: some View {
        HStack {
            Spacer()
            Text("08/10/2025")
                .font(.system(size: 14))
                .foregroundColor(.white.opacity(0.7))
        }
    }
}
